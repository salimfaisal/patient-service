spring:
  application:
    name: patient

  datasource:
    url: jdbc:postgresql://localhost:5432/patientdb
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false